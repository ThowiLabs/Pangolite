initShellControls();$('logoutBtn').addEventListener('click',async()=>{await api('/api/logout',{method:'POST',body:'{}'});location.href='/login'});setupForms();if(typeof setupLogsControls==='function')setupLogsControls();syncMode();syncOrigin();syncProtectionFields();syncEditProtectionFields();init().catch(e=>msg(e.message,true));
