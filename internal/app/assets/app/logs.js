async function loadLogs(){try{const data=await api('/api/system/logs?limit=300');logsLines=data.lines||[];const box=$('logsBox');if($('logsPath'))$('logsPath').textContent='Archivo: '+(data.path||'no configurado')+' · maximo '+(data.maxEntries||1000)+' entradas';if(!box)return;clearNode(box);if(!logsLines.length){box.textContent='Sin eventos registrados todavia.';return}logsLines.forEach(line=>{const node=tplNode('tpl-log-line')||document.createElement('span');node.textContent=line;if(line.includes('level=ERROR'))node.classList.add('error');if(line.includes('level=WARN'))node.classList.add('warn');box.appendChild(node)});box.scrollTop=box.scrollHeight}catch(err){if($('logsBox'))$('logsBox').textContent='No se pudieron cargar logs: '+err.message}}
async function copyLogs(btn){try{await copyText(logsLines.join('\n'));copyFeedback(btn,true)}catch(err){copyFeedback(btn,false);msg('No se pudieron copiar logs: '+err.message,true)}}
