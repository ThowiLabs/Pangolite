let csrf='';let force=true;const msg=document.getElementById('msg');
function show(t,bad=true){msg.className='alert '+(bad?'alert-danger':'alert-success');msg.textContent=t;msg.classList.remove('d-none')}
function escAuth(s){return String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function setAuthLoading(btn,label){if(!btn)return()=>{};const html=btn.innerHTML;btn.disabled=true;btn.setAttribute('aria-busy','true');btn.classList.add('btn-loading');btn.innerHTML='<span class="btn-loading-spinner" aria-hidden="true"></span><span>'+escAuth(label)+'</span>';return()=>{btn.innerHTML=html;btn.disabled=false;btn.removeAttribute('aria-busy');btn.classList.remove('btn-loading')}}
async function init(){const res=await fetch('/api/session');const data=await res.json();if(!data.authenticated){location.href='/login';return}csrf=data.csrfToken;force=!!data.user.forcePasswordChange;if(accountEmail)accountEmail.value=data.user.email||'';if(!force){currentGroup.classList.remove('d-none');currentPassword.required=true}}
passForm.addEventListener('submit',async e=>{e.preventDefault();msg.classList.add('d-none');if(newPassword.value!==confirmPassword.value){show('Las contraseñas no coinciden');return}const done=setAuthLoading(e.submitter,'Guardando');try{const res=await fetch('/api/password',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:JSON.stringify({currentPassword:currentPassword.value,newPassword:newPassword.value,email:accountEmail.value})});const data=await res.json();if(!res.ok)throw new Error(data.error||'No se pudo guardar la cuenta');show('Cuenta actualizada',false);setTimeout(()=>location.href='/',700)}catch(err){show(err.message);done()}})
init();
